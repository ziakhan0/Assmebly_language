[org 0x0100]
jmp start
string: db'Ziya_khan'
length: dw 9
clrscr:
     push bp
     mov bp,sp
     push ax
     push bx
     push cx
     push dx
     push di
     push es
     ; mov al,80
     ; mul byte[bp+4]
     ; add ax,[bp+6]
     ; shl ax,1
     mov di,0
     mov ax,0xb800
     mov es,ax
     mov cx,2000
     mov ax,0x0720      ;space
     cld
     rep stosw
     pop es
     pop di
     pop ax
     pop bx
     pop cx
     pop dx
     pop bp
     ret
printstr:
     push bp
     mov bp,sp
     push ax
     push bx
     push cx
     push dx
     push di
     push es
     mov ax,0xb800
     mov es,ax
     mov al,80
     mul byte[bp+8]
     add ax,[bp+10]
     shl ax,1
     mov di,ax
     mov ah,0x07
     mov si,[bp+6]      ;string in si
     mov cx,[bp+4]     ;length
     cld 
     nextchar:
          lodsb
          stosw
          loop nextchar
     pop es
     pop di
     pop dx
     pop cx
     pop bx
     pop ax
     pop bp
     ret 8





     



start:
    
     call clrscr
     mov ax,5    ;x-pos
     push ax
     mov ax,7     ;y-pos
     push ax
     mov ax,string   
     push ax
     mov ax,[length]
     push ax
     call printstr

mov ax,0x4c00
int 21h
